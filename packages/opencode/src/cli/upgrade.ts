// Offline build: update checks and self-update are permanently off.
// The original implementation contacted opencode.ai, registry.npmjs.org,
// brew/choco/scoop endpoints and api.github.com.
export async function upgrade() {}
